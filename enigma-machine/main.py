import numpy as np
from tempfile import TemporaryFile
#inputs = input()


def rotor(rotor):
    rotor1 = np.random.randint(26, size=67)
    a = rotor1 + 97
    x = list(a)
    print(x)

rotor(rotor)

